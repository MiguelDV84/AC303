<persistence xmlns="https://jakarta.ee/xml/ns/persistence" version="3.0">
    <persistence-unit name="unidadPersistenciaMDV" transaction-type="RESOURCE_LOCAL">
        <!-- Representamos las clases -->
        <class>org.mdv.model.Cliente</class>
        <class>org.mdv.model.Producto</class>
        <class>org.mdv.model.Categoria</class>
        <class>org.mdv.model.Venta</class>
        <class>org.mdv.model.VentaDetalle</class>

        <properties>
            <!-- === Conexión a MariaDB === -->
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/gestion-ventas"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value="root"/>
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>

            <!-- === Hibernate / DDL === -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MariaDBDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/> <!-- create, update, validate, create-drop -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>

    </persistence-unit>
</persistence>